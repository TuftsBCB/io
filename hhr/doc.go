/*
Package hhr provides routines for reading (but not writing) hhr files, which
are the output produced by hhsuite's hhsearch and hhblits programs.

Note that only the header information along with the hit list is parsed. The
alignments with secondary structure annotation are not (yet) parsed.
*/
package hhr
